
## PC3 Deployment — AgentSocial

**Time:** ~23:20 EDT

### What was done
- Installed pnpm, pm2 on PC3 (DESKTOP-QLDDUVF, `100.109.228.58`)
- Cloned AgentSocial repo (latest commit: `3f9e890`)
- Added root `package.json` for monorepo setup
- Copied `.env` files from PC2 (API + Web)
- Built API (0 TS errors) and Web (Next.js production build)
- Started services with PM2:
  - `agentsocial-api` — port 3001, PID 185194
  - `agentsocial-web` — port 3000, PID 185213
- PM2 save configured for auto-restart

### Access URLs (via Tailscale)
- **API**: `http://100.109.228.58:3001/api/v1/health` ✅
- **Web**: `http://100.109.228.58:3000` ✅
- Also accessible from PC2: `http://100.109.228.58:3001` and `http://100.109.228.58:3000`

### PC3 Specs
- Ubuntu 22.04 LTS (WSL2)
- 8 cores, 7.7GB RAM, 897GB free disk
- Node.js v22.22.1
- OpenClaw running on port 18789 (Maven + 26 agent workspaces)

### Voice-agent build skipped
- TS errors in `packages/voice-agent` (Square SDK type issues)
- Not needed for Maven's social dashboard — can fix separately

### Next Steps
- Update Maven agent config to point to `http://localhost:3001` or `http://100.109.228.58:3001`
- Verify Maven can call Zernio endpoints through PC3 API
- Set up PM2 startup script (needs sudo for systemd)
- Hetzner cloud deployment remains the long-term plan

## PC3 Deployment Complete — 23:27 EDT

### Services Running (PM2)
| Service | Port | Status | PID |
|---------|------|--------|-----|
| agentsocial-api | 3001 | ✅ Online | 186441 |
| agentsocial-web | 3000 | ✅ Online | 185213 |

### Maven Agent Access
- **API Base URL:** `http://localhost:3001/api/v1`
- **Dashboard:** `http://localhost:3000`
- **API Key:** `as_dev_GvRoZQfNEa9d_9s0n5pPkuUm`
- **Auth:** X-API-Key header or Bearer JWT token
- **User:** maven@agentsocial.ai / Maven Social Strategist
- **User ID:** `5ddc582b-e3eb-4193-bfa7-b7e23a9b041b`

### Verified Endpoints
- ✅ `GET /health` — returns `{"status":"ok"}`
- ✅ `GET /social/profiles` — returns 2 profiles (Default + PLEIJ)
- ✅ `GET /social/accounts?profileId=PLEIJ` — returns TikTok account
- ✅ `GET /social/accounts?profileId=Default` — returns GBP account
- ✅ `POST /auth/register` — user registration works
- ✅ `POST /auth/login` — JWT auth works
- ✅ `POST /api-keys` — API key generation works

### Bug Fixed
- API key auth prefix mismatch: `ak_` → `as_dev_` in auth.ts plugin
- Committed and pushed as `51ad556`

### Agent Config Updates on PC3
- Maven IDENTITY.md — added AgentSocial API + connected accounts
- Bridge IDENTITY.md — added AgentSocial API + connected accounts
- Social workspace TOOLS.md — full API reference with endpoints
- Social workspace MEMORY.md — deployment details

### Still TODO
- [ ] PM2 startup script (needs sudo on PC3)
- [ ] SSL/TLS via Tailscale Funnel or Caddy reverse proxy
- [ ] Custom domain (agentsocial.ai or *.clawstudio.co)
- [ ] Hetzner cloud for production (blocked on Jason)

---

## June 3 — Quiet Day

**Time:** 23:00 EDT

### Activity
- No new code commits on June 3. Last commit was `cc47e43` at 23:30 EDT June 2 (Maven onboarding guide).
- Only workspace file changes: MEMORY.md, HEARTBEAT.md, TOOLS.md, DREAMS.md — all from cron/auto maintenance.
- No feature development, bug fixes, or deployment changes.

### Status
- PC3 services presumed still running (PM2 auto-restart configured).
- All blocked items remain blocked: Hetzner account, DNS wildcard, Phorest credentials, GBP API reapply (July 22).
- Platform stable at 0 TS errors for API and Web.
